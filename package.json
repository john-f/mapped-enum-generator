{
  "name": "mapped-enum-generator",
  "version": "1.0.0",
  "description": "Generates enums from your Prisma schema, using mapped names as values",
  "bin": {
    "prisma-markdown": "lib/enum.generator.js"
  },
  "scripts": {
    "build": "rimraf lib && tsc",
    "dev": "rimraf lib && tsc --watch"
  },
  "devDependencies": {
    "@prisma/client": "^5.0.0",
    "@types/node": "^20.6.0",
    "prettier": "^3.0.3",
    "prisma": "^5.0.0",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@prisma/generator-helper": "^5.0.0"
  },
  "peerDependencies": {
    "@prisma/client": ">= 5.0.0",
    "prisma": ">= 5.0.0"
  },
  "files": [
    "lib",
    "src"
  ]
}
